{
  "name": "@code-monsters/asset-cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "pnpm run typecheck && node --test tests/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  }
}
