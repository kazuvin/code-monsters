# Sprite pipeline fixtures

Automated tests generate small synthetic high-resolution frames in temporary directories. Real AI outputs must not be used as deterministic CI fixtures.

When a reviewed regression image is added here, document its license, origin, expected background color, and the quality issue it covers.
